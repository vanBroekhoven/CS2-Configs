// Virtual numpad with Right ALT held
bind "RALT+p" "buy ak47; buy m4a1;"          // 7 position (AK/M4)
bind "RALT+[" "buy awp;"                     // 8 position (AWP)
bind "RALT+]" "buy m4a1_silencer"            // 9 position (M4A1-S)

bind "RALT+;" "buy mac10; buy mp9;"          // 4 position (SMGs)
bind "RALT+'" "buy ssg08;"                   // 5 position (Scout)
bind "RALT+\" "buy galilar; buy famas;"      // 6 position (FAMAS/Galil)

bind "RALT+/" "buy p250;"                    // 1 position (P250)
bind "RALT+." "buy deagle"                   // 2 position (Deagle)
bind "RALT+," "buy tec9; buy fiveseven;"     // 3 position (Tec9/Five-Seven)

// Don't forget grenades and armor
bind "RALT+o" "buy smokegrenade;"            // 0 position (Smoke)
bind "RALT+i" "buy flashbang;"               // * position (Flash)
bind "RALT+u" "buy hegrenade;"               // - position (HE)
bind "RALT+y" "buy molotov; buy incgrenade;" // + position (Molly)
bind "RALT+l" "buy vest;"                    // . position (Vest)
bind "RALT+k" "buy vesthelm;"                // Enter position (Helmet)
bind "RALT+j" "buy defuser;"                 // Additional (Defuser)